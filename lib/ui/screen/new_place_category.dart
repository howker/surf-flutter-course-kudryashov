import 'package:flutter/material.dart';

/// Экран выбора категории нового места
class NewPlaceCategoryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
